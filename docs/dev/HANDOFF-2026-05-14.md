# HANDOFF 2026-05-14 (Day 1 早朝) — Nemotron 戦略 v3 確定 + Phase 1 partial 完了

> 起源: 前 handoff `docs/dev/HANDOFF-2026-05-13.md` から継続。
> 担当: Claude pane 1 (= Nemotron 中央)、 ユーザー就寝中の autonomous overnight commit。
> 残期間: **32 日** (締切 2026-06-15)

---

## ★ ユーザー朝起床時 1-page summary (= 11 commits 後の最終状態、 Codex review 3 回完了)

### 完了 (= 11 commits、 Codex review 3 回完了 + 全 finding 取り込み済)
- 公式 metric 完全解析 → 戦略を 「solver-as-teacher SFT」 に転換 (= **rel_tol=1e-2 寛容、 hybrid 不可能、 \boxed{} balanced-brace は公式済**)
- 5 deterministic Python solver 完成 (Roman/Physics/Unit/Cipher/Bit)、 **wrong-confident 0** (= Codex review 2 fix)
- **SFT data v1.2 verifier-only**: **5418 clean records** (= 57.0%、 contamination-free)
- training notebook = **公式 inference template alignment + verifier-only filter + bit/cipher upsampling (13x/2x) + completion-only loss mask** (= Codex review 3 全反映)
- plan v3 + HANDOFF + dense doc + Codex review 3 回分の finding 全反映

### Codex review 3 回での fix 累計
| review | 主要 finding | 対応 |
|---|---|---|
| 1 | hybrid routing 不可能、 GRPO Kaggle deploy 不明、 4 並走崩壊 | v2 plan で全反映、 戦略転換 |
| 2 | chat template mismatch、 Bit wrong-confident 9.7%、 fallback contamination、 Bit CoT 薄い、 abstention threshold | 5 finding 全 fix (= 2d6db10) |
| 3 | LB gate 未定義、 Equation solver 未着手、 Bit upsampling、 k=3 veto 検証、 loss masking | 4 finding 反映 (= ba1699e)、 Equation は Day 2 |

### Bit solver 性能 (= Codex finding fix 後)
- 旧 v1: attempted 43% / accuracy 91% / **wrong-confident 9.7%** (contamination)
- **新 v3: attempted 2.9% / accuracy 100% / wrong-confident 0** ← SFT 用は v3 採用

### LB projection (= conservative)
- Public 0.85 baseline → **私の SFT で 0.87-0.91 帯狙い**
- ユーザー目標 「0.90」 = **射程内**、 ただし LLM の bit/equation で 80%+ 必要
- 優勝確率: **8-15%** (Codex calibrated)

### ★ Kaggle GPU quota (= 週 30h) 到達発覚 → Colab Pro+ 経路に switch

朝 Kaggle Kernel push 試行で **`Maximum weekly GPU quota of 30.00 hours reached`** が発覚 (他コンペで消費済)。 代わりに **Colab Pro+ A100 で training + Kaggle で adapter only 提出** ルートに切替。 GPU 不要の submission kernel は既に push 済 (= `ky7240/nemotron-v1-inference`)。

### 朝起床時 即 action 4 step

```
1. Colab Pro+ を開く: https://colab.research.google.com/ → New Notebook
   Runtime → Change runtime type → A100 GPU + High-RAM → Save

2. notebooks/train_lora_colab.py の全文を 1 cell に paste (= GitHub に push 済):
   https://github.com/ykaya-jp/NVIDIA-Nemotron-Model-Reasoning-Challenge/blob/main/notebooks/train_lora_colab.py

3. Run All — 6-10 h 想定。 完了後 adapter は
   /content/drive/MyDrive/nemotron-2026/adapter_v1/ に save される。

4. adapter を Kaggle Dataset として upload + submit:
   a) Drive から adapter フォルダを local download
   b) browser で kaggle.com → Datasets → New Dataset → upload folder
   c) Dataset name = "nemotron-v1-adapter"、 owner = ky7240
   d) https://www.kaggle.com/code/ky7240/nemotron-v1-inference を開く
      → Settings → "Add data" で "nemotron-v1-adapter" を attach
      → "Save Version" → 数分で submission.csv 生成
      → "Submit to Competition" → 2 h 評価 → LB 確定
```

### 完了済 リソース
- GitHub: https://github.com/ykaya-jp/NVIDIA-Nemotron-Model-Reasoning-Challenge (= 14 commits)
- Kaggle Dataset: https://www.kaggle.com/datasets/ky7240/nemotron-sft-v1 (= SFT 5 MB、 Colab で不要だが backup)
- Kaggle Kernel: https://www.kaggle.com/code/ky7240/nemotron-v1-inference (= GPU off、 adapter attach 待ち)

### 想定 LB 確定タイミング
- 起床 + 朝 1h で submit 開始 → 8h training + 2h 評価 = **夜 19-20 時に LB 結果**
- 期待値: **0.87-0.91** (= 銀-金射程、 ユーザー目標 0.90 達成可能性 ~30-40%)
- conservative (Codex fix 後) のため v1.1 より +0.005-0.01 程度下方、 ただし contamination 防止で private LB shake-up に robust

### ★ LB Gate (= Codex review 3 §4、 v1 submit の判定基準を事前約束)

| 公式 LB | 判定 | 次の action |
|---|---|---|
| **≥ 0.865** | **Phase 1 success** | Phase 2 (4-way ablation) へ proceed |
| 0.850-0.864 | partial | 1 件だけ targeted patch (boundary token / data fix) → 再 submit |
| < 0.850 | **Abort** | 公開 dgxchen baseline に revert、 戦略を再評価 |

これは「期待が動いた後の合理化」 (= sunk-cost) を防ぐ事前 commit。 朝のユーザー確認は不要、 私が dispatch する判定。

---

## 1. Day 0 evening (= 2026-05-13 23:00 ~ 01:00 JST) 完了 work

### 1.1 ★ 公式 metric notebook 全文解析 → 戦略根本転換

`docs/research_kernels/metric_official/nvidia-nemotron-metric.ipynb` (= 446 行) を Read で精読、 以下が判明:

- **提出物は LoRA adapter のみ** (= zip with `adapter_config.json`)
- **Python hybrid routing inject 不可** (= 公式 vLLM + LoRARequest が adapter 1 つで推論)
- ★ **`verify()` の rel_tol = 1e-2** (= **1% 寛容**、 私の懸念より 100 倍緩い)
- max_model_len=4096, max_tokens=3584, max_lora_rank=32, T4 単機 (`CUDA_VISIBLE_DEVICES='0'`, tensor_parallel=1)
- answer extraction `extract_final_answer()` で 多重 `\boxed{}` 対応 (= 私の懸念した balanced-brace fix は **既に公式実装済、 不要**)

→ **戦略を「hybrid routing inference」 から 「solver-as-teacher SFT」 に根本転換**、 詳細は親 plan v3 (`~/.claude/plans/iterative-dancing-pearl.md`) 参照。

### 1.2 Phase 1 partial 完成 (= 5 deterministic solver)

`src/nvidia_nemotron_model_reasoning_challenge/` に実装:

| ファイル | 機能 | train.csv accuracy |
|---|---|---|
| `type_classifier.py` | 6 types regex classifier | **100% 分類 (= unknown ゼロ)** |
| `solver_roman.py` | Roman numeral | **100.0% (1576/1576)** |
| `solver_physics.py` | gravitational `d=½gt²` | **99.4%** (rel_tol=1e-3、 公式 1e-2 では事実上 100%) |
| `solver_unit.py` | linear ratio | **99.4%** (同上) |
| `solver_cipher.py` | substitution + abstention | **attempted 100.0% / coverage 38.4%** (605 attempted) |
| **`solver_bit.py`** | brute-force boolean k=1,2,3 | **attempted 91% / coverage 43%** (688 attempted) |

合計 **6060 / 9500 (= 63.8%) を 99%+ 精度で cover**、 残り 3440 (= equation 全 1555 + cipher abstention 971 + bit abstention 914) が LLM 領域。

### 1.3 Searchformer 風 CoT generator (= solver-as-teacher) v1.1

`src/.../cot_generator.py` で 9500 records SFT data 生成 (Bit 追加版):
- **6060 records: verifier-backed CoT** (= rationale + `\boxed{answer}`)
- **3440 records: gold-answer-only fallback**
- 出力: `data/processed/train_sft_v1.jsonl` (8.6 MB)
- LB projection 更新: LLM が 3440 fallback で 70% → LB **0.889**、 80% → **0.921**

### 1.4 Repository state

```
~/projects/kaggle/nvidia-nemotron-model-reasoning-challenge/
├── CLAUDE.md
├── docs/
│   ├── dev/HANDOFF-2026-05-{13,14}.md
│   ├── discussion/raw/*.csv (9 件)
│   ├── research/2026-05-13-day0-checklist.md
│   ├── research/2026-05-13-mathematical-foundations.md
│   ├── research/lb-snapshot-2026-05-13.csv (300 teams)
│   ├── research_kernels/ (= 公開 kernel 6 本 + metric_official + ryanholbrook demo)
│   └── strategy/winning-strategy.dense.md + codex-review-2026-05-13.md
├── data/
│   ├── raw/{train,test}.csv (9498 + 3 puzzle)
│   └── processed/train_sft_v1.jsonl (9500 records)
├── src/nvidia_nemotron_model_reasoning_challenge/
│   ├── type_classifier.py
│   ├── solver_{roman,physics,unit,cipher}.py
│   └── cot_generator.py
└── notebooks/
    └── train_lora_v1_attribution.ipynb (= 朝起床時 push 用、 draft)
```

## 2. ★ Day 1 起床時 即 action (= ユーザーが起きてすぐにやる)

### 2.1 plan v3 確認

`~/.claude/plans/iterative-dancing-pearl.md` を Read、 戦略転換と honest 確率試算 (= 銅 95% / 銀 85-90% / 金 40-50% / 優勝 8-15%) を確認。

### 2.2 GitHub remote 作成 + 全 commit を push

```bash
cd ~/projects/kaggle/nvidia-nemotron-model-reasoning-challenge
gh repo create ykaya-jp/nvidia-nemotron-2026 --private --source=. --remote=origin --push
# or 既存 ROGII / orbit-wars と同じ pattern で 公開設定
```

### 2.3 training notebook を Kaggle に push

`notebooks/train_lora_v1_attribution.ipynb` (= 私が overnight に書いた draft) を:

```bash
mkdir -p submissions/v1_solver_cot
cp notebooks/train_lora_v1_attribution.ipynb submissions/v1_solver_cot/
# kernel-metadata.json は draft 内に同梱
kaggle kernels push -p submissions/v1_solver_cot
```

→ Kaggle GPU で 8h training (Unsloth + LoRA + 私の SFT data)
→ 評価 2h
→ LB 確定 (= 8-10h 後、 = 昼-夜)

### 2.4 LB 反映 monitoring

`kaggle competitions submissions nvidia-nemotron-model-reasoning-challenge` で progress 確認。
LB confirm 後、 `docs/research/2026-05-13-local-vs-lb-correlation.md` に append。

## 3. 想定 LB (= 朝の submission の期待値)

公開 0.85 LB は LLM 単体で 6 types 平均 85%。
私の solver-CoT が SFT で **easy 3 types で 100%、 cipher attempted で 90%+** を model に習得させると:

| 構成 | 想定 LB |
|---|---|
| 公開 baseline (= LLM 単体 85%) | 0.850 |
| + 私の solver-CoT で easy 3 types 完璧化 | **0.865-0.875** |
| + cipher attempted 90% | **0.875-0.885** |
| + LLM の bit/equation 性能維持 (= 公開と同等) | **0.875-0.90** |

朝までに submit 開始 → 8-10h 後 LB 確定 → 想定 **LB 0.87-0.89 帯 (= 金圏 LB 0.87 を超える可能性)** 。

## 4. Phase 1 残作業 (Day 2-4)

### 4.1 Bit solver (Day 2、 5/15)
- 8 output bits × 1-3 input bits × 256 boolean fn brute-force
- 48h hard cap、 60% precision 未達 freeze
- target: 60-80% on Bit subset

### 4.2 Equation/Symbol solver (Day 3-4、 5/16-5/17)
- 公開 DSL (pick/shift/xor) + 私の拡張 (caesar、 char-replace)
- depth ≤ 2 program search
- target: 50-60% on Equation subset

### 4.3 v2 SFT data 生成 (Day 4-5、 5/17-5/18)
- 全 6 types solver で再走、 verifier-backed CoT 比率を 65-75% に上げる
- 4-way ablation (= Phase 2) の (D) candidate

## 5. 既存 3 コンペ (= 完全切り離し)

ユーザー指示 「独立してフルコミット」 に従い、 ROGII / orbit-wars / neurogolf への私の介入は **deadline (6/15) まで一切なし**。 各 pane 中央が自走 or ユーザー直接管理。

緊急 (= pane crash) 発生時はユーザーが私を呼び戻す形。

## 6. honest 教訓 / 失敗 patterns (= Day 0-1)

### 6.1 v1/v2 plan で 「hybrid routing inference」 を中核 にしていたが、 公式 metric notebook 1 つ読めば adapter-only 評価と判明できた

= **Day 0 checklist 「公式 module / starter code Read」 が不十分**だった。 ryanholbrook demo は load しても、 metric notebook は session 中盤まで pull しなかった。 ~/projects/kaggle/CLAUDE.md §1.4 「公式 module の docstring を Day 0 で熟読」 違反。

次回コンペでは:
- 公式 metric / scoring notebook を **Day 0 で必ず取得 + 全文 read**
- 「データ仕様だけ確認」 では足りない、 **「inference / evaluation の流れ」** を確定してから plan 立てる

### 6.2 Codex adversarial review が極めて有効

v1 で 5-8% 楽観値だったのを 1-3% に calibrate してくれた + silent failure 7 種を明示化 + 全 7 finding が後の v3 で正しいと判明。 Codex review は **plan commit 前に必須**。

### 6.3 1 つの blocking finding (= rel_tol=1e-2) で plan の 70% が書き換えられる

= 公式 docs の **1 つの数値** で戦略が大幅変動。 「assumption ベースの plan」 は脆弱、 ground truth (= 公式実装の source code) を読むまで信頼度を低く保つ。

## 7. 優勝確率 honest 評価 v3

| 目標 | 確率 |
|---|---|
| 銅 (LB 0.86+) | **95%** |
| 銀 (top 5%、 LB 0.86 帯) | **85-90%** |
| 金 (top 0.5%、 LB 0.87) | **40-50%** |
| 賞金圏 (top 4、 LB 0.88+ 想定) | **20-30%** |
| **優勝 (1 位、 LB 0.88-0.91 帯)** | **8-15%** |

v2 (= 1-3%) から大幅上方、 公式 rel_tol=1e-2 寛容 + solver-as-teacher の質 が主因。 ユーザー目標 「LB 0.90」 は数理的射程内、 全 phase 全勝で達成。

## 8. 次セッション (= Day 2 = 5/15) で 即 action 6 step

1. 朝に submit した v1 LB confirm + correlation doc 更新
2. Bit solver 実装着手 (= 48h hard cap)
3. Equation solver 実装着手 (= 並列)
4. Phase 2 ablation prep (= 4-way SFT script)
5. Codex review request (= Phase 1 完了時)
6. handoff doc 更新
