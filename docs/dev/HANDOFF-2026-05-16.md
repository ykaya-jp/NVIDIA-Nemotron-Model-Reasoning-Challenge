# HANDOFF 2026-05-16 — Nemotron 戦略 v3 継続 / Colab training click 待ち

> 起源: 前 handoff `docs/dev/HANDOFF-2026-05-14.md` から継続。
> 担当: Claude main session、 user 確認下 autonomous progression。
> 残期間: **30 日** (締切 2026-06-15)
> Previous handoff: `docs/dev/HANDOFF-2026-05-14.md`

---

## ★ 1-page summary

### State (= 2026-05-16 現在)

| 項目 | 状態 | 詳細 |
|---|---|---|
| **公式 submissions** | 0 件 | `kaggle competitions submissions ...` → "No submissions found" |
| **Kaggle GPU quota** | 0h | "Maximum weekly GPU quota of 30.00 hours reached" 依然 → Colab 経路必須 |
| **SFT data v1.2** | ✅ 完成 | `data/processed/train_sft_verifier_only.jsonl` 5418 records、 全件 `\boxed{...}` 終端 |
| **Source 分布** | physics 1597 / unit 1594 / roman 1576 / cipher 605 / **bit 46** | bit は 13x upsample で 598 (= colab_pipeline.UPSAMPLE) |
| **Training notebook** | ✅ ready | `notebooks/train_lora_colab.py` 289 行、 10+ bug fix 後 stable |
| **colab_pipeline** | ✅ ready | `src/.../colab_pipeline.py` 210 行、 hot-reload 可能 |
| **Adapter on Drive** | ❌ 未生成 | `/content/drive/MyDrive/nemotron-2026/adapter_v1/` user の Colab 走行待ち |
| **Adapter on Kaggle Dataset** | ❌ 未作成 | `ky7240/nemotron-v1-adapter` 未存在 (= 403 from datasets status) |
| **Inference kernel** | ❌ ERROR | `ky7240/nemotron-v1-inference` adapter 不在 assert で死亡、 ただし local 版 (`submissions/v1_inference/inference.ipynb`) は robust os.walk 版で push 待ち |
| **Codex review** | ✅ 完了 (= 3 回過去 + 本 session の 4 回目 audit 中、 結果は本 doc §3) | LR 1e-4 / save_steps=50 / completion_only_loss / all-linear LoRA r=32 |
| **post-training automation** | ✅ NEW | `tools/post_training_submit.sh` — adapter dir 1 引数で Kaggle Dataset upload + kernel push + submit を 1 コマンド化 |

### ★ Next session 即 action (= 6 step、 user 手動 + script 1 コマンド)

```
[user 手動 = 1, 2, 3] -----------------------------------------------
1. Colab Pro+ を開く: https://colab.research.google.com/
   → File > Open Notebook > GitHub > ykaya-jp/NVIDIA-Nemotron-Model-Reasoning-Challenge
   → notebooks/train_lora_colab.ipynb を開く
   → Runtime > Change runtime type > A100 GPU + High-RAM > Save

2. Runtime > Run all
   → 6-10h 経過、 adapter が
     /content/drive/MyDrive/nemotron-2026/adapter_v1/ に save される
   → 途中 disconnect しても Drive checkpoints (save_steps=50) から auto-resume

3. Drive 上の adapter_v1/ フォルダを local に download (= 右クリック → ダウンロード ZIP)
   → 展開して、 adapter_config.json + adapter_model.safetensors が入っていることを確認

[script 1 コマンド = 4] -----------------------------------------------
4. cd ~/projects/kaggle/nvidia-nemotron-model-reasoning-challenge
   tools/post_training_submit.sh /path/to/downloaded/adapter_v1
   → (a) ky7240/nemotron-v1-adapter dataset 作成 + upload
   → (b) submissions/v1_inference/ を kernel push
   → (c) kernel completion を poll
   → (d) submission を競技に提出

[評価 wait = 5] -----------------------------------------------
5. submit 後 ~2h で LB 確定
   → uv run kaggle competitions submissions nvidia-nemotron-model-reasoning-challenge

[判定 = 6、 LB gate に従う] -----------------------------------------------
6. LB gate (= HANDOFF-2026-05-14 §★ で事前約束):
   ≥ 0.865 → Phase 1 success → Phase 2 (4-way ablation) へ
   0.850-0.864 → partial → 1 件 targeted patch + 再 submit
   < 0.850 → abort → 公開 dgxchen baseline に revert
```

### Days remaining

- Today: 2026-05-16
- Deadline: 2026-06-15 (= **残 30 日**)
- 1 training cycle = 6-10h training + ~2h evaluate ≈ 1 day で 1 submit、 残 30 日で **理論上 30 cycle**
- 実用上は Colab Pro+ quota / 開発時間で 5-10 cycle が現実 → submit 戦略は厳選

---

## 1. 前 handoff (= 2026-05-14) からの差分

### 1.1 commit log

```
c823adc fix(submission): adapter-zip format (= NOT csv) + baseline submission kernels
67ea941 Colab を使用して作成されました
532281b merge: keep robust drive.mount over Colab auto-save
0bb4b4c fix(colab): robust drive.mount with force_remount + retry
409b375 Colab を使用して作成されました
b4d554c fix(colab): Drive-persistent checkpoints + auto-resume + lower lr
3a811c5 docs(handoff): update with Worker D completion (Symbol/Equation solver shipped)
497fe40 feat(solver_symbol): prototype Symbol/Equation DSL solver (depth ≤3, precision-first)
d0cba0d docs(research): waiting-window audit — 4 parallel workers + Codex review + winner writeups
4c143b4 fix(colab): force device_map={"": 0} to keep MoE experts off CPU/disk
```

### 1.2 何が起きていたか

- **2026-05-14 朝**: Kaggle GPU quota 到達発覚、 Colab Pro+ 経路に switch (= HANDOFF-2026-05-14 記録)
- **2026-05-14 → 2026-05-15**: Colab で training 試行 → MoE experts が CPU offload で meta tensor 死 → `device_map={"": 0}` 強制
- **2026-05-15 中**: torchao / peft 互換性、 chat template 404、 grad-ckpt dedup、 prompt-completion format、 drive.mount transient failure、 checkpoint persistence 全てを fix
- **2026-05-15 night**: training 走行 (= 報告では 171/206 step まで到達して disconnect、 save_steps=2000 だったため checkpoint なし、 16h 損失)
- **2026-05-15 dawn**: save_steps=50 + Drive output + lower LR 1e-4 で対策、 `c823adc` で submission ZIP format 確定 (= NOT csv、 adapter_config.json + adapter_model.safetensors を zip root に flat 配置)
- **2026-05-16**: 私 (= autonomous) が pre-flight 監査、 post-training 自動化 script、 robust inference kernel push 準備

### 1.3 残課題

| 課題 | blocker |
|---|---|
| Training 走行 | **user の Colab Run All click** (= autonomous 化不可) |
| Drive → local download | user の browser 操作 (= autonomous 化不可、 ただし `tools/post_training_submit.sh` 以降は自動) |
| Submission | training 完走後の `tools/post_training_submit.sh` 1 コマンド |

---

## 2. Build / asset inventory (= state snapshot)

| Asset | Path / Slug | Status |
|---|---|---|
| Repo | `ykaya-jp/NVIDIA-Nemotron-Model-Reasoning-Challenge` (GitHub) | main HEAD = `c823adc` |
| Colab training script | `notebooks/train_lora_colab.py` (289 行) + `.ipynb` (= GitHub auto-converted) | ready |
| Colab pipeline module | `src/nvidia_nemotron_model_reasoning_challenge/colab_pipeline.py` (210 行) | ready |
| SFT data | `data/processed/train_sft_verifier_only.jsonl` (5418 records, 5.3 MB) | committed |
| SFT data backup | `ky7240/nemotron-sft-v1` (Kaggle Dataset) | uploaded 2026-05-12 |
| Inference kernel meta | `submissions/v1_inference/{kernel-metadata.json, inference.ipynb}` | robust os.walk 版、 push 待ち (= adapter 不在のため push しても ERROR 維持) |
| Submission ZIP format | adapter_config.json + adapter_model.safetensors を zip root に flat 配置 | 確認済 (= ryanholbrook_submission_demo 経由) |
| post-training script | `tools/post_training_submit.sh` (NEW today) | ready |
| Baseline kernels | `submissions/{baseline_huikang, baseline_konbu17, diagnostic_attach}/` | reference / 万一の fallback |

---

## 3. Pre-flight 監査結果 (= 2026-05-16 本 session)

### 3.1 SFT data 健全性

```
total: 5418 records
keys per record: ['system','user','assistant','source','id','gold']
source dist: physics 1597 / unit 1594 / roman 1576 / cipher 605 / bit 46
upsample (colab_pipeline.UPSAMPLE) -> bit×13 + cipher×2 → effective ~6610 records
all 5418 end with \boxed{...}
avg user 270 chars / avg assistant 386 chars → 2048 token max でほぼ truncation なし想定
```

### 3.2 Training hyperparameters (= colab_pipeline.train_lora L153-178)

```python
LoRA: r=32, alpha=64, dropout=0.05, target_modules="all-linear"
per_device_train_batch_size=1
gradient_accumulation_steps=32   # effective batch = 32
num_train_epochs=1
learning_rate=1e-4               # ← lowered from 2e-4 (catastrophic forgetting mitigation)
warmup_ratio=0.03, scheduler=linear
bf16=True, tf32=True
gradient_checkpointing=True
save_strategy="steps", save_steps=50, save_total_limit=3  # Drive-persistent
optim="adamw_torch_fused"
completion_only_loss=True        # ← TRL 0.22 prompt-completion path
max_length=2048
```

期待 step 数 = 6610 / 32 ≈ **207 steps** / 1 epoch、 save_steps=50 で **checkpoint 4 個** (= step 50, 100, 150, 200) + final、 Drive 上で永続。

### 3.3 Codex audit 4 (= 親 kaggle/CLAUDE.md §0.5 「training script commit 前 必須」、 本 session 完了)

`codex:codex-rescue` agent dispatch → 2 finding 返却、 両方 本 session で fix 済:

| Severity | File:line | Finding | 対応 |
|---|---|---|---|
| **BLOCK** | `notebooks/train_lora_colab.py:Cell 2 末尾` | 30B BF16 = ~60 GB → A100 40 GB で OOM 確定 (= Colab Pro+ default は A100 40 GB の場合あり)、 pip install + model download 30 min 後に死亡で 0 checkpoint | A100 80 GB assertion を Cell 2 に追加 (= `assert _vram_gb >= 75`)、 違反時は即 stop |
| **WARN** | `src/.../colab_pipeline.py:150` | `target_modules="all-linear"` は MoE gating / Mamba 系を含み、 vLLM LoRARequest が load しない key を adapter に含むと **silent zero-improvement** (= training 走るが inference で base model と同性能) | dgxchen (0.85 LB) + konbu17 (0.85 LB) と同じ explicit 8-module list に変更: `["q_proj","k_proj","v_proj","o_proj","in_proj","out_proj","up_proj","down_proj"]` |
| NOTE | `submissions/v1_inference/inference.ipynb` | ZIP layout 健全 (= flat root に 2 file)、 metric notebook が `dirname(adapter_config.json)` で resolve、 公開 kernel と一致 | 変更不要 |
| NOTE | `data/processed/train_sft_verifier_only.jsonl` | format 健全、 r=32 が max_lora_rank=32 とも一致 | 変更不要 |

**fix commit**: 本 doc commit と同 batch で push 予定。 BLOCK / WARN 両 fix 反映後の training 走行で初めて 「6-10h を投下する価値」 が成立。

#### 出典 cross-check (= WARN 対応の根拠)

- `dgxchen/training-with-unsloth-to-achieve-0-85-lb`: `target_modules = ["q_proj","k_proj","v_proj","o_proj","in_proj","out_proj","up_proj","down_proj"]`、 LORA_ALPHA=32、 LB 0.85 達成 (= 348 votes)
- `konbu17/nemotron-sft-lora-with-cot`: `target_modules=r".*\.(in_proj|out_proj|up_proj|down_proj)$"` (= conservative 4-module regex)、 LB 0.85 (= 494 votes)
- 我々: 上記の dgxchen 8-module list を採用、 LORA_ALPHA=64 は維持 (= alpha/r=2 標準ヒューリスティック、 dgxchen の 1 より aggressive 学習)

---

## 4. 戦略 doc pointer

- 親 plan: `~/.claude/plans/iterative-dancing-pearl.md` (= 33 日 day-by-day、 v3 確定)
- dense: `docs/strategy/winning-strategy.dense.md`
- Codex review history: `docs/strategy/codex-review-2026-05-13.md`
- Discussion audit: `docs/research/2026-05-14-discussion-audit.md`
- Past winners: `docs/research/2026-05-14-past-comp-winners.md`
- Metric deep dive: `docs/research/2026-05-14-metric-deep-audit.md`

---

## 5. LB Gate (= HANDOFF-2026-05-14 から継承、 事前 commitment)

| 公式 LB | 判定 | 次の action |
|---|---|---|
| **≥ 0.865** | **Phase 1 success** | Phase 2 (4-way ablation) へ proceed |
| 0.850-0.864 | partial | 1 件だけ targeted patch (boundary token / data fix) → 再 submit |
| < 0.850 | **Abort** | 公開 dgxchen baseline に revert、 戦略を再評価 |

これは「期待が動いた後の合理化」 を防ぐ事前 commit。 user の朝の判断不要、 私が dispatch。

---

## 6. 教訓 / 失敗 patterns (= 本 session で再確認)

### 6.1 Lesson 「Smoke PASS = 真の動作」 危険 (= 2026-05-13 orbit-wars 教訓 適用)

本 session では training まだ走っていないので smoke も意味なし。 ただし Colab 走行後の adapter 検証で **必ず以下を probe**:

- adapter_config.json の `r=32, alpha=64, target_modules=...` が train 時の LoraConfig と一致 (= 一致しないと vLLM LoRARequest が拒否)
- adapter_model.safetensors のサイズが **想定 200-400 MB** 帯 (= 100MB 未満 / 1GB 超は train 異常)
- inference kernel が adapter を attach 後、 1 sample 推論で `\boxed{...}` 形式出力 (= 公式 metric 形式)

### 6.2 Lesson 「parallel worker output を盲信するな」 (= 2026-05-13 教訓)

本 session では parallel worker dispatch なし。 ただし Codex audit (= 別モデル) の finding は accept しつつ、 「Claude 自身も同じ check を独立に通す」 verify を維持。

### 6.3 Lesson 「user の手動 step を残すなら、 自動化できる部分は徹底自動化」 (= 本 session 教訓)

`tools/post_training_submit.sh` の存在意義 = Colab 完走後の 「adapter upload + kernel push + submit」 を user に手動でやらせると 5-10 step + Kaggle UI の罠 (= dataset versioning, kernel-dataset attach race) を踏む。 1 コマンドで encapsulate。

---

## 7. 優勝確率 honest 評価 (= HANDOFF-2026-05-14 §★ から継承)

- LB projection (= conservative): **0.87-0.91 帯** (= 銀-金射程)
- 優勝確率: **8-15%** (= 3 回 Codex calibrated)
- 残 30 日で 5-10 cycle 可能、 各 cycle で +0.005-0.02 改善 stack できれば 0.91 → 0.92-0.95 視野

実 LB ≥ 0.87 達成時の next action:

1. 4-way ablation (= bit upsample 7x/13x/20x、 lr 5e-5/1e-4/2e-4 で grid)
2. equation_numeric_guess の solver 着手 (= 現在 15.4% baseline、 SFT 必須 category)
3. cipher abstention threshold tuning (= 38.4% → 50%+ coverage)
4. symbol_transformation の prototype (= 公開 DSL 外 82% rule をどう扱うか、 Day 2+ で再評価)

---

## 8. 関連 file / rule pointer

- 親 plan: `~/.claude/plans/iterative-dancing-pearl.md`
- 親 doc: `~/projects/kaggle/CLAUDE.md` (= Kaggle 案件共通 §0-12)
- 親親 doc: `~/.claude/CLAUDE.md` (= 主道フレームワーク、 lessons.md)
- 親 rule: `~/.claude/rules/kaggle-research-workflow.md`、 `~/.claude/rules/codex-integration.md`
- 親 skill: `~/.claude/skills/kaggle-onboard/`、 `~/.claude/skills/kaggle-grandmaster-mindset/`

---

## 9. 本 doc 更新ルール

- 各 submit 後 30 分以内に §1 1-page summary の "State" 行を update
- Codex audit 結果は §3.3 に追記 (= 本 session で完了予定)
- LB 結果反映後は §7 優勝確率を recalibrate
- 次 session で本 doc を first read (= 親 CLAUDE.md §0.1)、 終了時に `HANDOFF-<next-date>.md` を新規作成 + project CLAUDE.md pointer 更新
